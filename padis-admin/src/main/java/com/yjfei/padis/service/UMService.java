package com.yjfei.padis.service;

/*
@Service("umService")
@Slf4j
public class UMService {
	
    @Resource
    private UMUserService umUserService;
    @Resource
    private UMAttributeService umAttributeService;

    @Value("${um2.company.code}")
    private String companyCode;

    @Value("${um2.unit.code}")
    private String unitCode;

    public UMUser getumUser(String um) throws UMException {
        UMUser umUser = null;
        try {
            umUser = umUserService.queryUserById(um);
        } catch (Exception e) {
            log.error("get umuser error!", e);
        }
        return umUser;
    }


    public boolean authenticate(String um, String password) {

        boolean flag = false;
        try {
            flag = umUserService.authenticate(um, password);
        } catch (UMException e) {
            e.printStackTrace();
        }
        return flag;
    }
}
*/
